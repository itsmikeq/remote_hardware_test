<%= link_to "Remote Hardware Test", root_path, :class => 'brand' %>
<ul class="nav">
	<% if user_signed_in? %>
	<li>
		<%= link_to 'Edit account', edit_user_registration_path %>
	</li>
	<% if current_user.has_role? :admin %>
	<li>
		<%= link_to 'Admin', users_path %>
	</li>
	<%end%>
		<% if ( current_user.has_role?(:device_admin)|| current_user.has_role?(:admin) ) %>
		<!-- Device admin roles -->
	<li>
		<%= link_to( 'New Device Test Flow', new_upload_path(:follow_workflow=>true)) %>
	</li>
	<li>
		<%= link_to 'Devices', devices_path %>
	</li>
	<li>
		<%= link_to 'Uploads', uploads_path %>
	</li>
	<!-- END Device admin roles -->
	<% if current_user.has_role? :admin %>
	<li>
		<%= link_to 'Companies', companies_path %>
	</li>
	<li>
		<%= link_to 'Users', users_path %>
	</li>
	<% end %>

	<% else %>
	<li>
		<%= link_to 'Sign up', new_user_registration_path %>
	</li>
	<% end %>
		<% end %>
	<% if user_signed_in? %>
	<li>
		<%= link_to 'Logout', destroy_user_session_path, :method=>'delete' %>
	</li>
	<% else %>
	<li>
		<%= link_to 'Login', new_user_session_path %>
	</li>
	<% end %>

</ul>
